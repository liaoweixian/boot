<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.lwx.mybatisplus.mapper.UserMapper">
    <select id="selectAll" resultType="com.lwx.mybatisplus.domain.User">
        select * from user ${ew.customSqlSegment}
    </select>
    <select id="selectUserPage" resultType="com.lwx.mybatisplus.domain.User">
        select * from user ${ew.customSqlSegment}
    </select>
     <select id="selectUserMapsPage" resultType="java.util.Map">
        select * from user ${ew.customSqlSegment}
    </select>

</mapper>